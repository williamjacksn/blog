[project]
name = "blog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markdown>=3.10.3",
    "pelican>=4.12.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.3",
    "ty>=0.0.71",
]

[tool.ruff.lint]
select = ["ANN", "E", "F", "FURB", "I", "PERF", "RUF", "S", "UP"]
